
build/
.claude/
# Claude Code harness state (scheduled tasks, etc.) — local only.
*.db
*.db-*
*.db-journal
dist/
*.egg-info/
.env
.env.*
!.env.example
logs/
.mypy_cache/
# Place locally to run `space-monitor extract-taxonomy` / `load`.
*.pyc
__pycache__/
.pytest_cache/
.ruff_cache/
# Secrets — never commit
# Source workbook + analyst-curated outputs — kept out of git for privacy.
Space_Dashboard_Hardcopy.xlsx
.streamlit/secrets.toml
.venv/
*.xlsx

# Local DB backups — never commit
backups/
